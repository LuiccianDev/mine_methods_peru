---

---

<footer class="site-footer w-[80%] mx-auto border-l border-r border-white/20 bg-black/40">
  <div class="footer-glow"></div>

  <div class="footer-content">
    <!-- Top row: brand + nav -->
    <div class="footer-top">
      <div class="footer-brand">
        <span class="footer-logo-icon">⛏</span>
        <span class="footer-logo-text">MineríaPE</span>
        <p class="footer-tagline">
          Data-driven decision making<br />for sustainable mining strategy.
        </p>
      </div>

      <div class="footer-links-grid">
        <div class="footer-col">
          <h4>Navegación</h4>
          <a href="#methodology">Metodología</a>
          <a href="#charts">Análisis</a>
          <a href="#map">Mapa Interactivo</a>
          <a href="#synthesis">Síntesis Estratégica</a>
        </div>
        <div class="footer-col">
          <h4>Tecnologías</h4>
          <a href="#">Python + Pandas</a>
          <a href="#">Astro + React</a>
          <a href="#">ECharts + Leaflet</a>
          <a href="#">Tailwind CSS</a>
        </div>
      </div>
    </div>

    <!-- Divider -->
    <div class="footer-divider"></div>

    <!-- Bottom row: source + copyright -->
    <div class="footer-bottom">
      <p class="footer-source">
        <strong>Fuente:</strong>
      </p>
      <p class="footer-copy">
        &copy; {new Date().getFullYear()} · Análisis de Métodos de Extracción Minera en el Perú
      </p>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    position: relative;
    background: #050505;
    border-top: 1px solid rgba(255, 255, 255, 0.06);
    padding: 60px 24px 32px;
    overflow: hidden;
  }

  .footer-glow {
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 600px;
    height: 300px;
    background: radial-gradient(ellipse, rgba(192, 7, 7, 0.08) 0%, transparent 70%);
    pointer-events: none;
  }

  .footer-content {
    position: relative;
    z-index: 2;
  }

  /* --- Top row --- */
  .footer-top {
    display: flex;
    justify-content: space-between;
    gap: 48px;
    flex-wrap: wrap;
  }

  .footer-brand {
    max-width: 280px;
  }

  .footer-logo-icon {
    font-size: 1.4rem;
    margin-right: 8px;
  }

  .footer-logo-text {
    font-size: 1.15rem;
    font-weight: 700;
    color: #ffffff;
    letter-spacing: 0.5px;
  }

  .footer-tagline {
    margin-top: 16px;
    font-size: 0.88rem;
    color: #606060;
    line-height: 1.6;
  }

  /* --- Links grid --- */
  .footer-links-grid {
    display: flex;
    gap: 56px;
    flex-wrap: wrap;
  }

  .footer-col {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  .footer-col h4 {
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: #a0a0a0;
    margin: 0 0 6px;
  }

  .footer-col a {
    color: #606060;
    text-decoration: none;
    font-size: 0.85rem;
    transition: color 0.2s ease;
  }

  .footer-col a:hover {
    color: #ffffff;
  }

  /* --- Divider --- */
  .footer-divider {
    height: 1px;
    background: rgba(255, 255, 255, 0.06);
    margin: 40px 0 24px;
  }

  /* --- Bottom row --- */
  .footer-bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 12px;
  }

  .footer-source {
    font-size: 0.8rem;
    color: #505050;
    margin: 0;
  }

  .footer-source strong {
    color: #c1d65b;
  }

  .footer-copy {
    font-size: 0.78rem;
    color: #404040;
    margin: 0;
  }

  @media (max-width: 768px) {
    .footer-top {
      flex-direction: column;
      gap: 32px;
    }

    .footer-links-grid {
      gap: 32px;
    }

    .footer-bottom {
      flex-direction: column;
      align-items: flex-start;
    }
  }
</style>
